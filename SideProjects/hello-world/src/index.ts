let sales: number = 123_456_789;
let course: string = "TypeScript";
let is_published: boolean = true;

// remember me - a tuple is a fixed length array where each element has a particular type. Usually only useful if you have only a key/value pair

// remember me - enum
// const small = 1;
// const medium = 2;
// const large = 3;

// PascalCase
// the below is an enum, the values would be 0,1,2
// enum Size { Small , Medium, Large }

// you can also explicitly set it differently, values would be 1,2,3
// const enum Size {
//     Small = 1,
//     Medium,
//     Large,
// }

// let mySize: Size = Size.Medium;
// console.log(mySize);

let i = 0;
while (i < 4);
{
    // do something
    console.log(i);
    i++;
}
